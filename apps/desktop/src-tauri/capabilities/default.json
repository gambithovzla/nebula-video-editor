{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "Default window: core API and opener.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
